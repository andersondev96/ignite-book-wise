import { styled } from '@/stitches.config'

export const LatestRatingsContainer = styled('div', {
  display: 'flex',
  flexDirection: 'column',
})

export const LastRatingContainer = styled('div', {
  marginTop: '40px',
})
